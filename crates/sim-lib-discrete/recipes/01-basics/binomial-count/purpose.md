# Count combinations with a binomial coefficient

Compute the binomial coefficient `C(5, 2)` with `discrete/binomial`. The discrete-math
domain evaluates a real combinatorial count -- the number of 2-element subsets of a
5-element set is `10` -- a live computation, not a quoted descriptor.
