# release-plz manages synchronized versions across the workspace.
# publish=false harnesses stay private to their package manifests and are skipped automatically.
[workspace]
# One version for the workspace.
dependencies_update = true
git_release_enable = true
# release-plz computes dependency order for publishable crates.
